# Local SQLite build artifact (1GB+, regenerated by scripts/etl/06_build_sqlite.ts).
# Production reads from Turso, not this file — see db/README.md. Already gitignored;
# mirrored here because `vercel` CLI uploads working-tree files regardless of .gitignore.
db/properties.db
db/properties.db-*

# Local debug/dev artifacts
*.tsbuildinfo
.next/
coverage/
